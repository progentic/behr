# nginx

Reverse proxy configuration for the single VPS deployment.

This directory is empty in Phase A. The real `bhr-cms.conf` is written in
Phase Q — Production Operations Lock, once there is an API and set of
apps worth routing to.
